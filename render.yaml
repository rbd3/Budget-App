databases:
  - name: postgres
    databaseName: budget_app_production_reuy
    user: budget_app_production_reuy_user

services:
  - type: web
    name: budgetapp
    runtime: ruby
    plan: free
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: postgres
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false
  